大体步骤：

1. 下载azd 
            powershell -ex AllSigned -c "Invoke-RestMethod 'https://aka.ms/install-azd.ps1' -OutFile 'install-azd.ps1'; ./install-azd.ps1 -Version 'daily'"

            azd version
            azd auth login



2. 下载模板
            azd init -t todo-java-mongo -b staging

            issue1: 暂时不能使用带 aca ，terraform 后缀之类的


3. 下载runtime （可以忽略，需要验证）
            java
            py
            node
            net

4.  替换azure.yaml文件,准备script脚本 ： 
            文件：每个template自己写
            > azure.yaml 
            > sh
            > ps1




5.  执行hooks
            azd up
            azd package
            azd provision
            azd deploy
            azd restore
            azd down

            issue2: azd up 和 其他指令重复



issue3:测试不能覆盖每一个hook：
 

1.Test Local file hook     

2.Test inline script hook   

只能run一种


3.Test Azd vars

4.Interactive set

5.Run on "window" or "posix" 

6.Set the "continueOnError" flag

7.Hook File path is relative paths

8.Invalid file paths

9.Multiple hooks
